name: Laravel

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Set up PHP
      - name: Setup PHP
        uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
        with:
          php-version: '8.0'
          extensions: mbstring, xml, bcmath, sqlite3 # 必要なPHP拡張を追加

      # Copy .env file
      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"

      # Install Composer dependencies
      - name: Install Dependencies
        run: composer install --no-ansi --no-interaction --prefer-dist
        working-directory: ./src # composer.jsonが存在するディレクトリを指定

      # Generate application key
      - name: Generate key
        run: php artisan key:generate
        working-directory: ./src # artisanが存在するディレクトリを指定

      # Set directory permissions
      - name: Directory Permissions
        run: chmod -R 777 storage bootstrap/cache
        working-directory: ./src # ディレクトリを指定

      # Create SQLite database
      - name: Create Database
        run: |
          mkdir -p database
          touch database/database.sqlite
        working-directory: ./src # ディレクトリを指定

      # Run tests
      - name: Execute tests (Unit and Feature tests) via PHPUnit/Pest
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite
        run: php artisan test
        working-directory: ./src # ディレクトリを指定
